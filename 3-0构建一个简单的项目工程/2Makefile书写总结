1，在一个工程下，工程根目录需要有一个总的Makefile
	声明一些全局变量，尤其是路径变量
	调用各级子目录的Makefile，最后调用main目录的Makefile
	安装
	卸载
	清理
2，共用的common.mk文件
	源文件变量，头文件变量
	目标文件变量（判断是否生成普通的目标文件）
	库文件变量（在对应的子目录定义好全局变量）
	连接文件变量（在main目录定义）
	规则，使用ifeq或ifneq区别好生成不同文件的规则，避免冲突
3，各级子目录Makefile
	定义全局变量，要生成的文件的变量
	调用工程根目录下的common.mk
